spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhuxiang_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 132135
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false

app:
  auth:
    token-secret: integration-test-secret
    fixed-sms-code: "123456"
  upload:
    directory: ./target/test-uploads
