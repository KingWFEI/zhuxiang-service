spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:zhuxiang;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  auth:
    token-secret: integration-test-secret
    fixed-sms-code: "123456"
  upload:
    directory: ./target/test-uploads
